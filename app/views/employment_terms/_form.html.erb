<%= form_with(model: [@user, employment_term]) do |form| %>
  <% if employment_term.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(employment_term.errors.count, "error") %> prohibited this employment_term from being saved:</h2>

      <ul>
        <% employment_term.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :division_id %>
    <%= form.text_field :division_id %>
  </div>

  <div class="field">
    <%= form.label :position_id %>
    <%= form.text_field :position_id %>
  </div>

  <div class="field">
    <%= form.label :begin_date %>
    <%= form.date_field :begin_date %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
